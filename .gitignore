/target
**/*.rs.bk
Cargo.lock.bak

# PDFium prebuilt binary (tải bằng scripts/fetch-pdfium.ps1)
/pdfium/
/pdfium.dll
/pdfium.dll.lib

# QPDF prebuilt binary (tải bằng scripts/fetch-qpdf.ps1)
/qpdf/

# Tauri / node
/app/node_modules
/app/dist
/app/src-tauri/target

# Bản build portable tải về từ GitHub Release
/dist/

# Ảnh tạm khi test render
/tmp-out/
*.tmp.png

# File PDF thật của user (hồ sơ/kinh doanh cá nhân) — chỉ dùng test local,
# không đẩy lên repo công khai. PDF thả ở root cũng là file test của user
# (corpus/ đã track sẵn nên không ảnh hưởng).
/pdf-file-to-test/
/*.pdf
